{
  "name": "@harness-arena/protocol",
  "version": "0.1.0",
  "description": "Versioned Zod schemas for Harness Arena: events, battle specs, arena.yaml manifest, metrics, evaluation, API.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --root ../.."
  },
  "dependencies": {
    "zod": "^4.6.5"
  },
  "devDependencies": {}
}
