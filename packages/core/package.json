{
  "name": "@harness-arena/core",
  "version": "0.1.0",
  "description": "Battle engine: workspaces, process runner, event bus, metrics, local reports, redaction, state store.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --root ../.."
  },
  "dependencies": {
    "@harness-arena/protocol": "workspace:*",
    "@harness-arena/adapters": "workspace:*",
    "@harness-arena/harness": "workspace:*",
    "@harness-arena/evaluator": "workspace:*",
    "execa": "^10.0.1",
    "zod": "^4.6.5"
  },
  "devDependencies": {}
}
