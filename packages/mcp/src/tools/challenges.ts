import type { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js';
import type { ArenaContext } from '../context.js';

/** arena_create_challenge, arena_get_challenge. */
export function registerChallengesTools(_server: McpServer, _ctx: ArenaContext): void {
  // implemented by the challenges workstream
}
