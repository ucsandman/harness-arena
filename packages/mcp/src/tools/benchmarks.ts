import type { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js';
import type { ArenaContext } from '../context.js';

/** arena_list_benchmarks, arena_run_benchmark. */
export function registerBenchmarksTools(_server: McpServer, _ctx: ArenaContext): void {
  // implemented by the benchmarks workstream
}
