import type { Command } from 'commander';
import type { CliDeps } from '../deps.js';

/** arena tournament: show, play the pending matches of a tournament. */
export function registerTournamentCommands(_program: Command, _deps: CliDeps): void {
  {
    // implemented by the tournament workstream
  }
}
