import type { Command } from 'commander';
import type { CliDeps } from '../deps.js';

/** arena leaderboard, rating, profile, badge: read the server. */
export function registerLeaderboardCommands(_program: Command, _deps: CliDeps): void {
  {
    // implemented by the leaderboard workstream
  }
}
