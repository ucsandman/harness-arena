import type { Command } from 'commander';
import type { CliDeps } from '../deps.js';

/** arena challenge: create, show, list, run (accept) a challenge. */
export function registerChallengeCommands(_program: Command, _deps: CliDeps): void {
  {
    // implemented by the challenge workstream
  }
}
