# Agent instructions

A Codex-only harness fixture: no CLAUDE.md, no arena.yaml.

- Work in small steps and run the test suite after each one.
- Never guess a flag; read the installed package.
- Report the command you ran and its exit code.
